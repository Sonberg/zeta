// <auto-generated/>
using System;
using Zeta.Core;

namespace Zeta.Schemas;

/// <summary>
/// Extension methods for type-specific dictionary schema transformations.
/// </summary>
public static class DictionarySchemaExtensions
{
    /// <summary>
    /// Applies transformations to the string key schema for each key in the dictionary.
    /// </summary>
    public static DictionaryContextlessSchema<string, TValue> EachKey<TValue>(
        this DictionaryContextlessSchema<string, TValue> schema,
        Func<StringContextlessSchema, ISchema<string>> keyTransform)
    {
        var newKeySchema = keyTransform(Z.String());
        return schema.WithKeySchema(newKeySchema);
    }

    /// <summary>
    /// Applies transformations to the int key schema for each key in the dictionary.
    /// </summary>
    public static DictionaryContextlessSchema<int, TValue> EachKey<TValue>(
        this DictionaryContextlessSchema<int, TValue> schema,
        Func<IntContextlessSchema, ISchema<int>> keyTransform)
    {
        var newKeySchema = keyTransform(Z.Int());
        return schema.WithKeySchema(newKeySchema);
    }

    /// <summary>
    /// Applies transformations to the double key schema for each key in the dictionary.
    /// </summary>
    public static DictionaryContextlessSchema<double, TValue> EachKey<TValue>(
        this DictionaryContextlessSchema<double, TValue> schema,
        Func<DoubleContextlessSchema, ISchema<double>> keyTransform)
    {
        var newKeySchema = keyTransform(Z.Double());
        return schema.WithKeySchema(newKeySchema);
    }

    /// <summary>
    /// Applies transformations to the decimal key schema for each key in the dictionary.
    /// </summary>
    public static DictionaryContextlessSchema<decimal, TValue> EachKey<TValue>(
        this DictionaryContextlessSchema<decimal, TValue> schema,
        Func<DecimalContextlessSchema, ISchema<decimal>> keyTransform)
    {
        var newKeySchema = keyTransform(Z.Decimal());
        return schema.WithKeySchema(newKeySchema);
    }

    /// <summary>
    /// Applies transformations to the bool key schema for each key in the dictionary.
    /// </summary>
    public static DictionaryContextlessSchema<bool, TValue> EachKey<TValue>(
        this DictionaryContextlessSchema<bool, TValue> schema,
        Func<BoolContextlessSchema, ISchema<bool>> keyTransform)
    {
        var newKeySchema = keyTransform(Z.Bool());
        return schema.WithKeySchema(newKeySchema);
    }

    /// <summary>
    /// Applies transformations to the System.Guid key schema for each key in the dictionary.
    /// </summary>
    public static DictionaryContextlessSchema<System.Guid, TValue> EachKey<TValue>(
        this DictionaryContextlessSchema<System.Guid, TValue> schema,
        Func<GuidContextlessSchema, ISchema<System.Guid>> keyTransform)
    {
        var newKeySchema = keyTransform(Z.Guid());
        return schema.WithKeySchema(newKeySchema);
    }

    /// <summary>
    /// Applies transformations to the System.DateTime key schema for each key in the dictionary.
    /// </summary>
    public static DictionaryContextlessSchema<System.DateTime, TValue> EachKey<TValue>(
        this DictionaryContextlessSchema<System.DateTime, TValue> schema,
        Func<DateTimeContextlessSchema, ISchema<System.DateTime>> keyTransform)
    {
        var newKeySchema = keyTransform(Z.DateTime());
        return schema.WithKeySchema(newKeySchema);
    }

#if !NETSTANDARD2_0
    /// <summary>
    /// Applies transformations to the DateOnly key schema for each key in the dictionary.
    /// </summary>
    public static DictionaryContextlessSchema<DateOnly, TValue> EachKey<TValue>(
        this DictionaryContextlessSchema<DateOnly, TValue> schema,
        Func<DateOnlyContextlessSchema, ISchema<DateOnly>> keyTransform)
    {
        var newKeySchema = keyTransform(Z.DateOnly());
        return schema.WithKeySchema(newKeySchema);
    }

    /// <summary>
    /// Applies transformations to the TimeOnly key schema for each key in the dictionary.
    /// </summary>
    public static DictionaryContextlessSchema<TimeOnly, TValue> EachKey<TValue>(
        this DictionaryContextlessSchema<TimeOnly, TValue> schema,
        Func<TimeOnlyContextlessSchema, ISchema<TimeOnly>> keyTransform)
    {
        var newKeySchema = keyTransform(Z.TimeOnly());
        return schema.WithKeySchema(newKeySchema);
    }

#endif
    /// <summary>
    /// Applies transformations to the string value schema for each value in the dictionary.
    /// </summary>
    public static DictionaryContextlessSchema<TKey, string> EachValue<TKey>(
        this DictionaryContextlessSchema<TKey, string> schema,
        Func<StringContextlessSchema, ISchema<string>> valueTransform)
        where TKey : notnull
    {
        var newValueSchema = valueTransform(Z.String());
        return schema.WithValueSchema(newValueSchema);
    }

    /// <summary>
    /// Applies transformations to the int value schema for each value in the dictionary.
    /// </summary>
    public static DictionaryContextlessSchema<TKey, int> EachValue<TKey>(
        this DictionaryContextlessSchema<TKey, int> schema,
        Func<IntContextlessSchema, ISchema<int>> valueTransform)
        where TKey : notnull
    {
        var newValueSchema = valueTransform(Z.Int());
        return schema.WithValueSchema(newValueSchema);
    }

    /// <summary>
    /// Applies transformations to the double value schema for each value in the dictionary.
    /// </summary>
    public static DictionaryContextlessSchema<TKey, double> EachValue<TKey>(
        this DictionaryContextlessSchema<TKey, double> schema,
        Func<DoubleContextlessSchema, ISchema<double>> valueTransform)
        where TKey : notnull
    {
        var newValueSchema = valueTransform(Z.Double());
        return schema.WithValueSchema(newValueSchema);
    }

    /// <summary>
    /// Applies transformations to the decimal value schema for each value in the dictionary.
    /// </summary>
    public static DictionaryContextlessSchema<TKey, decimal> EachValue<TKey>(
        this DictionaryContextlessSchema<TKey, decimal> schema,
        Func<DecimalContextlessSchema, ISchema<decimal>> valueTransform)
        where TKey : notnull
    {
        var newValueSchema = valueTransform(Z.Decimal());
        return schema.WithValueSchema(newValueSchema);
    }

    /// <summary>
    /// Applies transformations to the bool value schema for each value in the dictionary.
    /// </summary>
    public static DictionaryContextlessSchema<TKey, bool> EachValue<TKey>(
        this DictionaryContextlessSchema<TKey, bool> schema,
        Func<BoolContextlessSchema, ISchema<bool>> valueTransform)
        where TKey : notnull
    {
        var newValueSchema = valueTransform(Z.Bool());
        return schema.WithValueSchema(newValueSchema);
    }

    /// <summary>
    /// Applies transformations to the System.Guid value schema for each value in the dictionary.
    /// </summary>
    public static DictionaryContextlessSchema<TKey, System.Guid> EachValue<TKey>(
        this DictionaryContextlessSchema<TKey, System.Guid> schema,
        Func<GuidContextlessSchema, ISchema<System.Guid>> valueTransform)
        where TKey : notnull
    {
        var newValueSchema = valueTransform(Z.Guid());
        return schema.WithValueSchema(newValueSchema);
    }

    /// <summary>
    /// Applies transformations to the System.DateTime value schema for each value in the dictionary.
    /// </summary>
    public static DictionaryContextlessSchema<TKey, System.DateTime> EachValue<TKey>(
        this DictionaryContextlessSchema<TKey, System.DateTime> schema,
        Func<DateTimeContextlessSchema, ISchema<System.DateTime>> valueTransform)
        where TKey : notnull
    {
        var newValueSchema = valueTransform(Z.DateTime());
        return schema.WithValueSchema(newValueSchema);
    }

#if !NETSTANDARD2_0
    /// <summary>
    /// Applies transformations to the DateOnly value schema for each value in the dictionary.
    /// </summary>
    public static DictionaryContextlessSchema<TKey, DateOnly> EachValue<TKey>(
        this DictionaryContextlessSchema<TKey, DateOnly> schema,
        Func<DateOnlyContextlessSchema, ISchema<DateOnly>> valueTransform)
        where TKey : notnull
    {
        var newValueSchema = valueTransform(Z.DateOnly());
        return schema.WithValueSchema(newValueSchema);
    }

    /// <summary>
    /// Applies transformations to the TimeOnly value schema for each value in the dictionary.
    /// </summary>
    public static DictionaryContextlessSchema<TKey, TimeOnly> EachValue<TKey>(
        this DictionaryContextlessSchema<TKey, TimeOnly> schema,
        Func<TimeOnlyContextlessSchema, ISchema<TimeOnly>> valueTransform)
        where TKey : notnull
    {
        var newValueSchema = valueTransform(Z.TimeOnly());
        return schema.WithValueSchema(newValueSchema);
    }

#endif
    /// <summary>
    /// Applies transformations to the string key schema for each key in the dictionary.
    /// </summary>
    public static DictionaryContextSchema<string, TValue, TContext> EachKey<TValue, TContext>(
        this DictionaryContextSchema<string, TValue, TContext> schema,
        Func<StringContextlessSchema, ISchema<string, TContext>> keyTransform)
    {
        var newKeySchema = keyTransform(Z.String());
        return schema.WithKeySchema(newKeySchema);
    }

    /// <summary>
    /// Applies transformations to the int key schema for each key in the dictionary.
    /// </summary>
    public static DictionaryContextSchema<int, TValue, TContext> EachKey<TValue, TContext>(
        this DictionaryContextSchema<int, TValue, TContext> schema,
        Func<IntContextlessSchema, ISchema<int, TContext>> keyTransform)
    {
        var newKeySchema = keyTransform(Z.Int());
        return schema.WithKeySchema(newKeySchema);
    }

    /// <summary>
    /// Applies transformations to the double key schema for each key in the dictionary.
    /// </summary>
    public static DictionaryContextSchema<double, TValue, TContext> EachKey<TValue, TContext>(
        this DictionaryContextSchema<double, TValue, TContext> schema,
        Func<DoubleContextlessSchema, ISchema<double, TContext>> keyTransform)
    {
        var newKeySchema = keyTransform(Z.Double());
        return schema.WithKeySchema(newKeySchema);
    }

    /// <summary>
    /// Applies transformations to the decimal key schema for each key in the dictionary.
    /// </summary>
    public static DictionaryContextSchema<decimal, TValue, TContext> EachKey<TValue, TContext>(
        this DictionaryContextSchema<decimal, TValue, TContext> schema,
        Func<DecimalContextlessSchema, ISchema<decimal, TContext>> keyTransform)
    {
        var newKeySchema = keyTransform(Z.Decimal());
        return schema.WithKeySchema(newKeySchema);
    }

    /// <summary>
    /// Applies transformations to the bool key schema for each key in the dictionary.
    /// </summary>
    public static DictionaryContextSchema<bool, TValue, TContext> EachKey<TValue, TContext>(
        this DictionaryContextSchema<bool, TValue, TContext> schema,
        Func<BoolContextlessSchema, ISchema<bool, TContext>> keyTransform)
    {
        var newKeySchema = keyTransform(Z.Bool());
        return schema.WithKeySchema(newKeySchema);
    }

    /// <summary>
    /// Applies transformations to the System.Guid key schema for each key in the dictionary.
    /// </summary>
    public static DictionaryContextSchema<System.Guid, TValue, TContext> EachKey<TValue, TContext>(
        this DictionaryContextSchema<System.Guid, TValue, TContext> schema,
        Func<GuidContextlessSchema, ISchema<System.Guid, TContext>> keyTransform)
    {
        var newKeySchema = keyTransform(Z.Guid());
        return schema.WithKeySchema(newKeySchema);
    }

    /// <summary>
    /// Applies transformations to the System.DateTime key schema for each key in the dictionary.
    /// </summary>
    public static DictionaryContextSchema<System.DateTime, TValue, TContext> EachKey<TValue, TContext>(
        this DictionaryContextSchema<System.DateTime, TValue, TContext> schema,
        Func<DateTimeContextlessSchema, ISchema<System.DateTime, TContext>> keyTransform)
    {
        var newKeySchema = keyTransform(Z.DateTime());
        return schema.WithKeySchema(newKeySchema);
    }

#if !NETSTANDARD2_0
    /// <summary>
    /// Applies transformations to the DateOnly key schema for each key in the dictionary.
    /// </summary>
    public static DictionaryContextSchema<DateOnly, TValue, TContext> EachKey<TValue, TContext>(
        this DictionaryContextSchema<DateOnly, TValue, TContext> schema,
        Func<DateOnlyContextlessSchema, ISchema<DateOnly, TContext>> keyTransform)
    {
        var newKeySchema = keyTransform(Z.DateOnly());
        return schema.WithKeySchema(newKeySchema);
    }

    /// <summary>
    /// Applies transformations to the TimeOnly key schema for each key in the dictionary.
    /// </summary>
    public static DictionaryContextSchema<TimeOnly, TValue, TContext> EachKey<TValue, TContext>(
        this DictionaryContextSchema<TimeOnly, TValue, TContext> schema,
        Func<TimeOnlyContextlessSchema, ISchema<TimeOnly, TContext>> keyTransform)
    {
        var newKeySchema = keyTransform(Z.TimeOnly());
        return schema.WithKeySchema(newKeySchema);
    }

#endif
    /// <summary>
    /// Applies transformations to the string value schema for each value in the dictionary.
    /// </summary>
    public static DictionaryContextSchema<TKey, string, TContext> EachValue<TKey, TContext>(
        this DictionaryContextSchema<TKey, string, TContext> schema,
        Func<StringContextlessSchema, ISchema<string, TContext>> valueTransform)
        where TKey : notnull
    {
        var newValueSchema = valueTransform(Z.String());
        return schema.WithValueSchema(newValueSchema);
    }

    /// <summary>
    /// Applies transformations to the int value schema for each value in the dictionary.
    /// </summary>
    public static DictionaryContextSchema<TKey, int, TContext> EachValue<TKey, TContext>(
        this DictionaryContextSchema<TKey, int, TContext> schema,
        Func<IntContextlessSchema, ISchema<int, TContext>> valueTransform)
        where TKey : notnull
    {
        var newValueSchema = valueTransform(Z.Int());
        return schema.WithValueSchema(newValueSchema);
    }

    /// <summary>
    /// Applies transformations to the double value schema for each value in the dictionary.
    /// </summary>
    public static DictionaryContextSchema<TKey, double, TContext> EachValue<TKey, TContext>(
        this DictionaryContextSchema<TKey, double, TContext> schema,
        Func<DoubleContextlessSchema, ISchema<double, TContext>> valueTransform)
        where TKey : notnull
    {
        var newValueSchema = valueTransform(Z.Double());
        return schema.WithValueSchema(newValueSchema);
    }

    /// <summary>
    /// Applies transformations to the decimal value schema for each value in the dictionary.
    /// </summary>
    public static DictionaryContextSchema<TKey, decimal, TContext> EachValue<TKey, TContext>(
        this DictionaryContextSchema<TKey, decimal, TContext> schema,
        Func<DecimalContextlessSchema, ISchema<decimal, TContext>> valueTransform)
        where TKey : notnull
    {
        var newValueSchema = valueTransform(Z.Decimal());
        return schema.WithValueSchema(newValueSchema);
    }

    /// <summary>
    /// Applies transformations to the bool value schema for each value in the dictionary.
    /// </summary>
    public static DictionaryContextSchema<TKey, bool, TContext> EachValue<TKey, TContext>(
        this DictionaryContextSchema<TKey, bool, TContext> schema,
        Func<BoolContextlessSchema, ISchema<bool, TContext>> valueTransform)
        where TKey : notnull
    {
        var newValueSchema = valueTransform(Z.Bool());
        return schema.WithValueSchema(newValueSchema);
    }

    /// <summary>
    /// Applies transformations to the System.Guid value schema for each value in the dictionary.
    /// </summary>
    public static DictionaryContextSchema<TKey, System.Guid, TContext> EachValue<TKey, TContext>(
        this DictionaryContextSchema<TKey, System.Guid, TContext> schema,
        Func<GuidContextlessSchema, ISchema<System.Guid, TContext>> valueTransform)
        where TKey : notnull
    {
        var newValueSchema = valueTransform(Z.Guid());
        return schema.WithValueSchema(newValueSchema);
    }

    /// <summary>
    /// Applies transformations to the System.DateTime value schema for each value in the dictionary.
    /// </summary>
    public static DictionaryContextSchema<TKey, System.DateTime, TContext> EachValue<TKey, TContext>(
        this DictionaryContextSchema<TKey, System.DateTime, TContext> schema,
        Func<DateTimeContextlessSchema, ISchema<System.DateTime, TContext>> valueTransform)
        where TKey : notnull
    {
        var newValueSchema = valueTransform(Z.DateTime());
        return schema.WithValueSchema(newValueSchema);
    }

#if !NETSTANDARD2_0
    /// <summary>
    /// Applies transformations to the DateOnly value schema for each value in the dictionary.
    /// </summary>
    public static DictionaryContextSchema<TKey, DateOnly, TContext> EachValue<TKey, TContext>(
        this DictionaryContextSchema<TKey, DateOnly, TContext> schema,
        Func<DateOnlyContextlessSchema, ISchema<DateOnly, TContext>> valueTransform)
        where TKey : notnull
    {
        var newValueSchema = valueTransform(Z.DateOnly());
        return schema.WithValueSchema(newValueSchema);
    }

    /// <summary>
    /// Applies transformations to the TimeOnly value schema for each value in the dictionary.
    /// </summary>
    public static DictionaryContextSchema<TKey, TimeOnly, TContext> EachValue<TKey, TContext>(
        this DictionaryContextSchema<TKey, TimeOnly, TContext> schema,
        Func<TimeOnlyContextlessSchema, ISchema<TimeOnly, TContext>> valueTransform)
        where TKey : notnull
    {
        var newValueSchema = valueTransform(Z.TimeOnly());
        return schema.WithValueSchema(newValueSchema);
    }

#endif
}
