// <auto-generated/>
using System;
using Zeta.Core;

namespace Zeta.Schemas;

/// <summary>
/// Extension methods for type-specific collection schema transformations.
/// </summary>
public static class CollectionSchemaExtensions
{
    /// <summary>
    /// Applies transformations to the string element schema for each item in the collection.
    /// </summary>
    public static CollectionContextlessSchema<string> Each(
        this CollectionContextlessSchema<string> schema,
        Func<StringContextlessSchema, ISchema<string>> elementTransform)
    {
        var newElementSchema = elementTransform(Z.String());
        return new CollectionContextlessSchema<string>(newElementSchema, schema.GetRules());
    }

    /// <summary>
    /// Applies transformations to the int element schema for each item in the collection.
    /// </summary>
    public static CollectionContextlessSchema<int> Each(
        this CollectionContextlessSchema<int> schema,
        Func<IntContextlessSchema, ISchema<int>> elementTransform)
    {
        var newElementSchema = elementTransform(Z.Int());
        return new CollectionContextlessSchema<int>(newElementSchema, schema.GetRules());
    }

    /// <summary>
    /// Applies transformations to the double element schema for each item in the collection.
    /// </summary>
    public static CollectionContextlessSchema<double> Each(
        this CollectionContextlessSchema<double> schema,
        Func<DoubleContextlessSchema, ISchema<double>> elementTransform)
    {
        var newElementSchema = elementTransform(Z.Double());
        return new CollectionContextlessSchema<double>(newElementSchema, schema.GetRules());
    }

    /// <summary>
    /// Applies transformations to the decimal element schema for each item in the collection.
    /// </summary>
    public static CollectionContextlessSchema<decimal> Each(
        this CollectionContextlessSchema<decimal> schema,
        Func<DecimalContextlessSchema, ISchema<decimal>> elementTransform)
    {
        var newElementSchema = elementTransform(Z.Decimal());
        return new CollectionContextlessSchema<decimal>(newElementSchema, schema.GetRules());
    }

    /// <summary>
    /// Applies transformations to the bool element schema for each item in the collection.
    /// </summary>
    public static CollectionContextlessSchema<bool> Each(
        this CollectionContextlessSchema<bool> schema,
        Func<BoolContextlessSchema, ISchema<bool>> elementTransform)
    {
        var newElementSchema = elementTransform(Z.Bool());
        return new CollectionContextlessSchema<bool>(newElementSchema, schema.GetRules());
    }

    /// <summary>
    /// Applies transformations to the System.Guid element schema for each item in the collection.
    /// </summary>
    public static CollectionContextlessSchema<System.Guid> Each(
        this CollectionContextlessSchema<System.Guid> schema,
        Func<GuidContextlessSchema, ISchema<System.Guid>> elementTransform)
    {
        var newElementSchema = elementTransform(Z.Guid());
        return new CollectionContextlessSchema<System.Guid>(newElementSchema, schema.GetRules());
    }

    /// <summary>
    /// Applies transformations to the System.DateTime element schema for each item in the collection.
    /// </summary>
    public static CollectionContextlessSchema<System.DateTime> Each(
        this CollectionContextlessSchema<System.DateTime> schema,
        Func<DateTimeContextlessSchema, ISchema<System.DateTime>> elementTransform)
    {
        var newElementSchema = elementTransform(Z.DateTime());
        return new CollectionContextlessSchema<System.DateTime>(newElementSchema, schema.GetRules());
    }

#if !NETSTANDARD2_0
    /// <summary>
    /// Applies transformations to the DateOnly element schema for each item in the collection.
    /// </summary>
    public static CollectionContextlessSchema<DateOnly> Each(
        this CollectionContextlessSchema<DateOnly> schema,
        Func<DateOnlyContextlessSchema, ISchema<DateOnly>> elementTransform)
    {
        var newElementSchema = elementTransform(Z.DateOnly());
        return new CollectionContextlessSchema<DateOnly>(newElementSchema, schema.GetRules());
    }

    /// <summary>
    /// Applies transformations to the TimeOnly element schema for each item in the collection.
    /// </summary>
    public static CollectionContextlessSchema<TimeOnly> Each(
        this CollectionContextlessSchema<TimeOnly> schema,
        Func<TimeOnlyContextlessSchema, ISchema<TimeOnly>> elementTransform)
    {
        var newElementSchema = elementTransform(Z.TimeOnly());
        return new CollectionContextlessSchema<TimeOnly>(newElementSchema, schema.GetRules());
    }

#endif
    /// <summary>
    /// Applies transformations to the string element schema for each item in the collection.
    /// </summary>
    public static CollectionContextSchema<string, TContext> Each<TContext>(
        this CollectionContextSchema<string, TContext> schema,
        Func<StringContextlessSchema, ISchema<string, TContext>> elementTransform)
    {
        var newElementSchema = elementTransform(Z.String());
        return new CollectionContextSchema<string, TContext>(newElementSchema, schema.GetRules());
    }

    /// <summary>
    /// Applies transformations to the int element schema for each item in the collection.
    /// </summary>
    public static CollectionContextSchema<int, TContext> Each<TContext>(
        this CollectionContextSchema<int, TContext> schema,
        Func<IntContextlessSchema, ISchema<int, TContext>> elementTransform)
    {
        var newElementSchema = elementTransform(Z.Int());
        return new CollectionContextSchema<int, TContext>(newElementSchema, schema.GetRules());
    }

    /// <summary>
    /// Applies transformations to the double element schema for each item in the collection.
    /// </summary>
    public static CollectionContextSchema<double, TContext> Each<TContext>(
        this CollectionContextSchema<double, TContext> schema,
        Func<DoubleContextlessSchema, ISchema<double, TContext>> elementTransform)
    {
        var newElementSchema = elementTransform(Z.Double());
        return new CollectionContextSchema<double, TContext>(newElementSchema, schema.GetRules());
    }

    /// <summary>
    /// Applies transformations to the decimal element schema for each item in the collection.
    /// </summary>
    public static CollectionContextSchema<decimal, TContext> Each<TContext>(
        this CollectionContextSchema<decimal, TContext> schema,
        Func<DecimalContextlessSchema, ISchema<decimal, TContext>> elementTransform)
    {
        var newElementSchema = elementTransform(Z.Decimal());
        return new CollectionContextSchema<decimal, TContext>(newElementSchema, schema.GetRules());
    }

    /// <summary>
    /// Applies transformations to the bool element schema for each item in the collection.
    /// </summary>
    public static CollectionContextSchema<bool, TContext> Each<TContext>(
        this CollectionContextSchema<bool, TContext> schema,
        Func<BoolContextlessSchema, ISchema<bool, TContext>> elementTransform)
    {
        var newElementSchema = elementTransform(Z.Bool());
        return new CollectionContextSchema<bool, TContext>(newElementSchema, schema.GetRules());
    }

    /// <summary>
    /// Applies transformations to the System.Guid element schema for each item in the collection.
    /// </summary>
    public static CollectionContextSchema<System.Guid, TContext> Each<TContext>(
        this CollectionContextSchema<System.Guid, TContext> schema,
        Func<GuidContextlessSchema, ISchema<System.Guid, TContext>> elementTransform)
    {
        var newElementSchema = elementTransform(Z.Guid());
        return new CollectionContextSchema<System.Guid, TContext>(newElementSchema, schema.GetRules());
    }

    /// <summary>
    /// Applies transformations to the System.DateTime element schema for each item in the collection.
    /// </summary>
    public static CollectionContextSchema<System.DateTime, TContext> Each<TContext>(
        this CollectionContextSchema<System.DateTime, TContext> schema,
        Func<DateTimeContextlessSchema, ISchema<System.DateTime, TContext>> elementTransform)
    {
        var newElementSchema = elementTransform(Z.DateTime());
        return new CollectionContextSchema<System.DateTime, TContext>(newElementSchema, schema.GetRules());
    }

#if !NETSTANDARD2_0
    /// <summary>
    /// Applies transformations to the DateOnly element schema for each item in the collection.
    /// </summary>
    public static CollectionContextSchema<DateOnly, TContext> Each<TContext>(
        this CollectionContextSchema<DateOnly, TContext> schema,
        Func<DateOnlyContextlessSchema, ISchema<DateOnly, TContext>> elementTransform)
    {
        var newElementSchema = elementTransform(Z.DateOnly());
        return new CollectionContextSchema<DateOnly, TContext>(newElementSchema, schema.GetRules());
    }

    /// <summary>
    /// Applies transformations to the TimeOnly element schema for each item in the collection.
    /// </summary>
    public static CollectionContextSchema<TimeOnly, TContext> Each<TContext>(
        this CollectionContextSchema<TimeOnly, TContext> schema,
        Func<TimeOnlyContextlessSchema, ISchema<TimeOnly, TContext>> elementTransform)
    {
        var newElementSchema = elementTransform(Z.TimeOnly());
        return new CollectionContextSchema<TimeOnly, TContext>(newElementSchema, schema.GetRules());
    }

#endif
    /// <summary>
    /// Applies transformations to the object element schema for each item in the collection.
    /// Allows inline object schema building for complex types with context awareness.
    /// </summary>
    public static CollectionContextSchema<TElement, TContext> Each<TElement, TContext>(
        this CollectionContextSchema<TElement, TContext> schema,
        Func<ObjectContextlessSchema<TElement>, ObjectContextSchema<TElement, TContext>> elementTransform)
        where TElement : class
    {
        var newElementSchema = elementTransform(Z.Object<TElement>());
        return new CollectionContextSchema<TElement, TContext>(newElementSchema, schema.GetRules());
    }

}
